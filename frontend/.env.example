# Optional deployed API address. Omit when running locally: Vite proxies /api to FastAPI.
VITE_API_BASE_URL=
# Optional browser-restricted Google Maps key. The local app also detects
# GOOGLE_MAPS_BROWSER_API_KEY from the project-root .env.
VITE_GOOGLE_MAPS_API_KEY=
